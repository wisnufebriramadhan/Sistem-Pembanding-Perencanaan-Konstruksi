<!doctype html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>SP2K</title><link rel="stylesheet" href="{{ asset('css/app.css') }}"></head><body><aside><a class="brand" href="{{ route('dashboard') }}">SP<span>2</span>K<small>Estimasi Konstruksi</small></a><nav><a href="{{ route('dashboard') }}">Ringkasan</a><a href="{{ route('boq-imports.create') }}">1. Upload BoQ</a><a href="{{ route('boq-imports.create') }}">2. Pilih wilayah & sumber</a><a href="{{ route('boq-imports.create') }}">3. Cari Harga</a>@if (in_array(auth()->user()->role, ['admin', 'reviewer']))<a href="{{ route('price-candidates.index') }}">4. Review kandidat</a>@endif<a href="#">5. Estimasi</a></nav><div class="profile">{{ auth()->user()->name }}<small>{{ strtoupper(auth()->user()->role) }}</small>@if (auth()->user()->role === 'admin')<small><a href="{{ route('reference-prices.index') }}">Admin: override Bank Harga</a> · <a href="{{ route('price-sources.index') }}">Sumber</a></small>@endif<form method="POST" action="{{ route('logout') }}">@csrf<button>Keluar</button></form></div></aside><main><header><div><p class="eyebrow">Sistem Pembanding Perencanaan Konstruksi</p><h1>@yield('title', 'Ringkasan')</h1></div><div class="date">{{ now()->translatedFormat('l, d F Y') }}</div></header>@yield('content')</main></body></html>
